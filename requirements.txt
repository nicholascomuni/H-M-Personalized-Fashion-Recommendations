# The notebook was developed in 2022 in Jupyter (Python 3.9) with the Kaggle competition data.
# Exact versions from that run were not recorded; these are compatible ranges, not a tested lock file.
# Upper bound: TensorFlow Recommenders relies on tf.keras (Keras 2), which TensorFlow 2.16+ replaces with Keras 3.
tensorflow>=2.8,<2.16
tensorflow-recommenders>=0.6,<0.8
numpy>=1.20
pandas>=1.3
pyarrow>=6.0
scikit-learn>=1.0
